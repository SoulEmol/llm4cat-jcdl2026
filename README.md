# AI for Organizing Digital Collections

Website for the JCDL 2026 half-day tutorial, **AI for Organizing Digital Collections: Practical Approaches with LLMs and Vision-Language Models**.

## Preview locally

The site has no build step. Open `index.html` directly in a browser, or run a small local server:

```powershell
python -m http.server 8000
```

Then visit <http://localhost:8000>.

## Publish with GitHub Pages

1. Create a public GitHub repository and push these files to its default branch.
2. Open the repository's **Settings → Pages**.
3. Under **Build and deployment**, select **Deploy from a branch**.
4. Choose the default branch and the `/ (root)` folder, then save.

The site will be available at `https://<account>.github.io/<repository>/` after GitHub finishes deploying it.

## Content to update before the event

- Add presenter profile links if desired.
- Confirm the tutorial room and exact start/end time.
- Add links for slides, Colab notebooks, sample data, and the reading list.
- Add the tutorial paper or DOI when it is available.
